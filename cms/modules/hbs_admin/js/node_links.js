(function($) {
  //Drupal.behaviors.hbs_admin.link_change = {
  //  attach: function(context, settings) {
  //    settings.hbs_admin = settings.hbs_admin || Drupal.settings.hbs_admin;
  //    $('a[href^="/node/add/"]').attr('href', function(i, h) {
  //      return h + (h.indexOf('?') != -1 ? "&context=" + settings.hbs_admin.original_path : "?context=" + settings.hbs_admin.original_path);
  //    });
  //  }
  //};
})(jQuery);